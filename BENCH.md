# 流水线评测结论（备份）

样本：B 站 [BV1FnGA66EPP](https://www.bilibili.com/video/BV1FnGA66EPP/)（约 28.8 分钟中文访谈）。对照稿为平台字幕（CER 用字符编辑距离，专名表含 SGLang / RadixArk / KV Cache / Miles 等）。原始 JSON、音频、跑次目录已从仓库移除。

【说话人N】与 `--names` 均按首次出场顺序对应（见 `srt_to_text.py`）。评测当时未指定 `--names` 曾按时长编号，已改回。

## 采纳进默认流水线

| 改动 | 结论 | 依据 |
|------|------|------|
| Python API + community-1，chunk 30；修 WhisperX 把 `language` 写成 `en` 导致中文空格 | 保留 | 墙钟 106.8s → 73.9s（RTFx 16.2 → 23.4）；中文词间空格率 0.487 → 0.009；拉丁词 0 → 340；关键词命中 0 → 139。说话人数仍为 4。该次对比 optimized 用了 batch 16，生产默认 batch 8（约 8GB 显存）。 |
| 同说话人短停顿才粘段（0.75s / 180 字） | 保留 | CER 不变 0.1302；最长段 2246 → 271 字；条数 25 → 78（未超过 250 上限）。 |
| exclusive 说话人分离（空则回退 overlapping） | 保留 | CER 与人数不变；Ethan「华尔街」窗口仍是独立说话人，未并进主持。 |
| 标题/简介/标签热词 | 保留 | CER 0.1302 → 0.1272；专名召回 0.167 → 0.678（SGLang 1→32，RadixArk 0→10）。 |
| 再加章节标题热词 | 保留 | 相对「仅标题简介标签」：CER 0.1272 → 0.1187；召回 0.678 → 0.789；KV Cache 0→10。 |

## 评测后未进默认

| 改动 | 结论 |
|------|------|
| 热词串音替换（Kiwi Cash → KV Cache 等规则表） | 持平，无额外增益 |
| 同源 URL 官方字幕优先于 ASR | 该 B 站链接拉不到字幕；不能把 YouTube 镜像当默认 |

## 说话人份额（community-1，未指定 --names）

约 1301s / 193s / 136s / 19.5s（约 79% / 12% / 8% / 1.2%）。第四人占比很小但是真人片段，未做合并。
